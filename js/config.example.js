// Vorlage für den lokalen Fallback: als js/config.js kopieren und den eigenen Schlüssel eintragen.
// js/config.js wird nicht committet. Auf Vercel den Key als Umgebungsvariable OPENWEATHER_KEY setzen.
// Ohne Key läuft das Regenradar nur mit RainViewer.
window.DASHBOARD_CONFIG = {
  openWeatherKey: "",
};
