// Vorlage: als js/config.js kopieren und den eigenen Schlüssel eintragen.
// js/config.js wird nicht committet. Ohne Schlüssel läuft das Regenradar nur mit RainViewer.
window.DASHBOARD_CONFIG = {
  openWeatherKey: "",
};
